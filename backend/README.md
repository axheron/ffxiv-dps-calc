# Backend for dps calculator

## Running locally

### Install dependencies

From inside this directory, run `pip install -r requirements.txt`

### Run local server

`python main.py` runs the server on `localhost:5000`